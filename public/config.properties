# ==============================================
# 保底进度配置文件
# 每行一条，格式：key = value（# 开头为注释行）
#
# 进度计算规则：
#   百分比 = (已消耗纠缠之缘数量 * 160 + 剩余原石数量) / 14400 * 100%
#   以 14400 原石为 100%，结果保留两位小数
#
# name 为可选配置，用于页面上显示的角色名；不填则默认显示 userA / userB
# ==============================================

# ---- userA ----
userA.fates = 6
userA.primogems = 400
userA.name = 旅行者A

# ---- userB ----
userB.fates = 11
userB.primogems = 0
userB.name = 旅行者B
